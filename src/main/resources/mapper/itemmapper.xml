<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.hong.mapper.ItemMapper">
      <select id="getAllItem" parameterType="HashMap" resultType="HashMap">
          SELECT i.item_id, i.item_detail, i.item_name, i.item_sell_status, i.price, i.stock_number, im.img_url
          FROM item i
          INNER JOIN item_img im
          ON i.item_id = i.item_id
      </select>
    <select id="searchItem" parameterType="itemDto" resultType="HashMap">
        SELECT * FROM item WHERE item_name LIKE CONCAT('%',#{item_name},'%')
    </select>
</mapper>